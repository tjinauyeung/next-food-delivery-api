head
  link(rel="stylesheet", href="/css/style.css", data-inline)

h2 #{date}
h3 nummer: #{id}

section
  h4 Bestelling
  table
    tbody
      each item in order
        tr
          td(class="type") #{item.type}
          td #{item.count} x
          td #{item.label}
          td(style='text-align: right') #{item.total}

      if deliveryCost
        tr
          td(colspan='3') bezorgkosten
          td(style='text-align: right') #{deliveryCost}

      tr
        td(colspan='3')
          strong Totaal
        td(style='text-align: right')
          strong #{price}

section
  h4 Klant
  p #{user.email}
  p #{user.phone}
  p #{user.address}
  p #{user.postalCode} #{user.city}

section
  h4 Bezorgen / Ophalen
  p(style='text-transform: uppercase') #{user.orderMethod}

section
  h4 Tijd
  p #{user.deliveryTime}

section
  if user.comments
    h4 Opmerkingen
    p #{user.comments}
